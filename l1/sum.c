#include <stdio.h>

int main()
{
    int x, y, z, sum;
    scanf("%d", &x);
    scanf("%d", &y);
    scanf("%d", &z);

    sum = x + y + z;
    printf("The sum of all numbers is %d\n", sum);

    return 0;
}